import styled from 'styled-components';

export const JoinUsContainer = styled.main`
  width: 100%;
  padding-top: 60px;
  
  .clock-box {
    max-width: 1080px;
    margin: 0 auto;
  }
  
  .see-rule {
    color: var(--base_orange);
    font-size: 18px;
  }
`;
